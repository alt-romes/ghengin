signature Ghengin.Core.Renderer.RenderPass where

import Ghengin.Core.Render.Monad

import Data.Counted

-- import Ghengin.Vulkan.RenderPass
--     ( createSimpleRenderPass,
--       destroyRenderPass,
--       VulkanRenderPass(_renderPass) )

data RenderPass

createSimpleRenderPass :: MonadRenderer m
                       => m (RefC RenderPass) -- If we always need this as a reference, it's easier to just return it as such here (avoids exposing a free function)


